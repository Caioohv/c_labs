#include <stdio.h>

int main() {
  
  // this is a comment
  // this does not affect the script
  // is just information 

  /*
    this is also a comment
    but used for many lines

    below, 'printf' shows the text
    between quotes in the terminal

    the return 0 is used to say
    'this code executed successfully'
    :)

    return 1, for example, means that
    it had an error :/
  */

  printf("Hello World!");

  return 0;
}